# Stage 19W12 frontend — val-act.html

Marker:

```text
latex-stage19w12-val-act-policy-foundation-20260602-1
```

Adds a separate admin/research UI:

```text
val-act.html
```

The UI can:

```text
- check backend value/action policy status
- build a time-indexed dataset from the OpenReview review corpus
- train baseline value/action models
- load the 25-action taxonomy
- inspect inferred action-label examples
- run ad-hoc predictions from paper + review + rebuttal text
```

The main LatexAI editor marker is also bumped to the 19W12 marker.
