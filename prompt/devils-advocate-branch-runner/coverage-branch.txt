Section coverage mode: selected branch target only.
Requested target sections/units: {{requestedTargets}}.
For every requested target section/unit, the final editor step must provide at least one patch-style coverage block.

Equation-focused override:
- If MATH EQUATION COVERAGE MODE ACTIVE appears later in the prompt, the final editor must prioritize the equation inventory and output one explanatory block per listed equation id.
- Equation explanations must use `% Target equation id: eq_###` so the frontend can place them immediately below the corresponding equation.
- In equation mode, do not drift into citation-only, related-work-only, or introduction-only edits.

If no edit is needed for a section, use exactly:
\lai{%
% Target section: <exact section title>
\emph{No edits recommended.}
}

If an edit is needed, do NOT output advice text like "Insert after" or "Replace the paragraph with". Output actual LaTeX-ready inserted text or a true \laiold{old}\lai{new} replacement pair. Put the target label as a LaTeX comment inside the block.

Equation explanation block format:
\lai{%
% Target equation id: <exact equation id>
% Target section: <containing section/unit>
<actual LaTeX-ready explanation to appear immediately below that equation>
}

Deduplication rule: if multiple agents propose the same edit, the final editor should keep one best version only and should not output shorter paraphrases of the same recommendation. Earlier debate agents should not output final \lai or \laiold blocks.
