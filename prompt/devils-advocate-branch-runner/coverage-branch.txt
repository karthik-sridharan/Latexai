Section coverage mode: selected branch target only.
Requested target sections/units: {{requestedTargets}}.
For every requested target section/unit, the final editor step must provide at least one raw LaTeX block patch when an actionable edit is useful.

Equation-focused override:
- If MATH EQUATION COVERAGE MODE ACTIVE appears later in the prompt, the final editor must prioritize the equation inventory and output one explanatory block per listed equation id.
- Equation explanations must target the listed equation id using TARGET_BLOCK_ID: eq_### with OPERATION: insert_after_block.
- In equation mode, do not drift into citation-only, related-work-only, or introduction-only edits.

If no edit is needed for a section, the final editor should return OPERATION: no_edit in the raw block patch protocol.

If an edit is needed, the final editor must output actual LaTeX-ready inserted/replacement content inside BEGIN_NEW_LATEX. Do not output Latexai internal change-marker macros; the compiler adds all wrappers.

Deduplication rule: if multiple agents propose the same edit, the final editor should keep one best version only and should not output shorter paraphrases of the same recommendation. Earlier debate agents should not output final raw patch blocks.

Safety rule: Never include source-context scaffolding in visible edits. Do not output `[important excerpt from this section]`, `[section ending excerpt]`, `[section excerpt truncated by Latexai]`, legacy source-context begin/end markers, or literal `\n` backslash-n text inside BEGIN_NEW_LATEX.
