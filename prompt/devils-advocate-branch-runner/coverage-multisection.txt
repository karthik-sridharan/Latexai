Section coverage mode: {{modeName}}.
Requested target sections/units: {{requestedTargets}}.
Whole-paper targeting active: {{wholePaperActive}}.

Coverage rule for the final editor step:
- For EVERY requested target section/unit, output AT LEAST ONE visible coverage/edit block.
- If edits are needed for a target, output one or more substantively distinct PATCH-STYLE blocks for that target; normally no more than two non-overlapping blocks per target.
- If no edits are needed for a target, output exactly one no-edit block for that target; do not also output edits for that target.
- Use the exact target section/unit names listed above so the insertion step can distribute blocks correctly.

Equation-focused override:
- If MATH EQUATION COVERAGE MODE ACTIVE appears later in the prompt, the editor must prioritize the equation inventory and output one explanatory block per listed equation id.
- Equation explanations must target the listed equation id using TARGET_BLOCK_ID: eq_### with OPERATION: insert_after_block.
- In equation mode, do not drift into citation-only, related-work-only, or introduction-only edits.

Raw patch protocol requirements:
- The editor must use LATEXAI_BLOCK_PATCH_BEGIN / LATEXAI_BLOCK_PATCH_END blocks.
- Put metadata in PATCH_ID, OPERATION, TARGET_BLOCK_ID, TARGET_SECTION, and RATIONALE fields.
- Put only the actual visible LaTeX content between BEGIN_NEW_LATEX and END_NEW_LATEX.
- Do not output Latexai internal change-marker macros. The deterministic compiler adds all old/new wrappers.
- Do not prefix visible paper text with %.

Deduplication rule: repeated citation-gap, related-work, assumption, novelty, limitation, positioning, or equation-explanation edits should be consolidated by the final editor into one best raw patch per target whenever possible. Earlier debate agents should discuss candidates, but only the final editor should output final raw patch blocks.

Safety rule: Never include source-context scaffolding in visible edits. Do not output `[important excerpt from this section]`, `[section ending excerpt]`, `[section excerpt truncated by Latexai]`, legacy source-context begin/end markers, or literal `\n` backslash-n text inside BEGIN_NEW_LATEX.
