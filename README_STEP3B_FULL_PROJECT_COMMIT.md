# Step 3B: full-project GitHub commits

Upload/replace all files in this zip.

New behavior:
- Adds a top-left `GitHub Full Commit` panel.
- `Preview commit files` shows every file that will be committed.
- `Commit All Files` sends the full discovered project, not just the active editor file.
- Subdirectory paths are preserved.
- Files added through the frontend should be included after the app stores them; tap `Save Now` or `Refresh file list`, then `Preview commit files`.

Open:
https://karthik-sridharan.github.io/Latexai/?v=stage3b-full-project-commit-1
