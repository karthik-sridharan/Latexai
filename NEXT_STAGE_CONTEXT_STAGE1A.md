# Next stage context — Lumina LaTeX Editor

Recommended Stage 1B priorities:

1. Replace textarea with CodeMirror 6 or another robust editor wrapper.
2. Add PDF download button and preserve most recent compiled PDF in memory/local cache.
3. Make compile errors clickable by parsing TeX log file/line references.
4. Add true binary file upload transfer to backend compile route.
5. Add import zip support, likely through a small bundled zip library or backend import.
6. Add project snapshots/version history.
7. Add more templates: article, beamer, homework, lecture notes, theorem-heavy paper.
8. Add source/PDF sync markers if using SyncTeX in backend.
