# Stage 19M1 — Frontend Real-Agent Result Cleaner

Changed file:

- `developer-debate-branches.html`

The developer page now shows a Stage 19M1 cleaner/validator panel after a Stage 19M real-agent run. It displays:

- validated `\lai` blocks;
- insertable blocks;
- rejected malformed candidates;
- citation keys detected;
- warnings;
- an insertable LaTeX draft.

New page controls:

- `Clean/validate last real-agent result`
- `Copy clean curl`

Open with cache busting after deploy:

`/developer-debate-branches.html?v=19m1`
