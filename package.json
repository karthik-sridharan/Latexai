{
  "name": "lumina-latex-backend-stage1c",
  "version": "1.1.0",
  "description": "Optional backend for Lumina LaTeX Editor Stage 1C: AI provider proxy plus job-based TeX compile pipeline.",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "node server.mjs",
    "dev": "node --watch server.mjs",
    "check": "node --check server.mjs"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.4.1"
  },
  "engines": {
    "node": ">=20"
  }
}
